

class CookieClient {
    constructor(){

    }
    setToken = (res, tokens) => {
        const accessToken = tokens.accessToken;
        const refreshToken = tokens.refreshToken;

        res.set({

        })
    }
}